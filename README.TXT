This code is used for prototype the experiment about feature segmentation.

Layout:

--baseline:
  "Baseline x-vector results and prototype recipe to 
   extract segmented feature."

--gmm-ubm-feats:
  "A GMM-UBM model based on mfcc feature."
